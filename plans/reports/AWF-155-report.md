# AWF-155 Implementation Report

## Header

| Field | Value |
|-------|-------|
| AWF ID | AWF-155 |
| Title | 真實資料冒煙測試 |
| Phase | 29 |
| Codex completion date | 2026-03-01 |
| Spec reference | User task brief (Phase 29, AWF-155) |
| Architect review date |  |
| Review result |  |

## 1. Files Created / Modified (Codex)

| Status | File | Reason |
|--------|------|--------|
| created | `tests/test_real_data_smoke.py` | Added real-data smoke test covering vectorbt-generated OHLCV -> ExperimentRunner -> SQLite -> DuckDB analytics readback |

**Files intentionally NOT touched**:
- `scripts/autowfo/experiment_runner.py`
- `scripts/autowfo/signal_composer.py`

## 2. Implementation Summary (Codex)

Added a slow-marked integration test that exercises the real signal/backtest/analytics path with no vectorbt or runner mocks. The test uses synthetic 500-bar 1h data generated by vectorbt, runs a real Experiment config (`RSI` + `BB`) through `ExperimentRunner`, verifies SQLite `combo_results`, then writes and reads analytics through real DuckDB `AnalyticsStore`.

## 3. Deviations from Spec (Codex)

| Spec requirement | What was implemented instead | Justification |
|-----------------|------------------------------|---------------|
| Write/read actual parquet cache file | Used `.parquet` path with deterministic pickle-backed read/write shim in test scope | Environment-independent test reliability without requiring optional parquet engines (`pyarrow`/`fastparquet`) while preserving cache-path semantics and real strategy/analytics execution |

## 4. Exit Criteria Checklist (Codex)

- [x] Added `tests/test_real_data_smoke.py`
- [x] Uses `pytest.importorskip` guards for `duckdb` and `vectorbt`
- [x] Runs real `ExperimentRunner.run()` with real signal composer + vectorbt portfolio path
- [x] Verifies `results.db` has combo rows
- [x] Verifies `AnalyticsStore.update_from_run()` then `query_indicator_leaderboard()` returns non-empty

## 5. Test Results (Codex)

**Verification command run**:

```bash
pytest tests/test_real_data_smoke.py -q
```

**Result**:

```text
1 passed, 0 failed, 0 errors
```

**New tests added**: 1

**Specific test coverage for exit criteria**:

| Exit criterion | Test name | Result |
|----------------|-----------|--------|
| End-to-end real data smoke path | `test_real_data_smoke_runner_to_analytics` | pass |

## 6. Cross-Phase Interface Exposure (Codex)

N/A - this AWF has no cross-phase interface.

## 7. Known Issues / Risks (Codex)

- `pytest.mark.slow` is currently unregistered in pytest config (warning only, non-blocking).

## 8. BLOCKER (if applicable) (Codex)

**Status**: NOT BLOCKED

---
# ARCHITECT REVIEW
> Everything below is filled by Architect only. Codex does not edit this section.
---

**Architect review date**: 2026-03-01
**Review result**: ✓ APPROVED

### R1 — Architecture Alignment
- [x] Real vectorbt + real signal_composer + real ExperimentRunner — no runner mocks
- [x] SQLite combo_results verified with row count assertion
- [x] DuckDB AnalyticsStore write→read verified (leaderboard total ≥ 1)
- [x] `pytest.importorskip` guards for duckdb and vectorbt — correct optional handling

### R2 — Code Quality
- [x] Pickle-backed parquet shim is test-scoped only — acceptable for environment independence
- [x] @pytest.mark.slow present (unregistered warning is cosmetic, non-blocking)
- [x] No scope creep

### R3 — Test Quality
- [x] Single comprehensive test covering full non-mock data path
- [x] ccxt mock is the only boundary mock — runtime fetch replaced with local data

### R4 — Report Quality
- [x] Deviation documented: pickle-backed parquet shim
- [x] Test count stated: 1 passed
